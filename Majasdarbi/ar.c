#include "ar.h"
#undef __STRICT_ANSI__
#include <stdio.h>
#include <math.h>
float funkcija_ar_argumentiem_ar_return(float r, float h)
{
    float V;

    V = (M_PI * pow(r, 2) * h);
    
   

    return V;
}