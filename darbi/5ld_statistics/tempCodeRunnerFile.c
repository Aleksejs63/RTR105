FILE * pFile;
    
    // pFile = fopen ("sort.dat", "w");
    // fprintf(pFile, "burts\tfrekvence\n");

    // for (int j = 0; j < a+1; j++)
    // {
    //     fprintf(pFile, "%c\t\t\t%d\n", chars[j], freq[j]);
    // }


    // fclose (pFile);